import { RepoInput, VulnerabilityDisplay } from "../components"

export const Home = () => {
    return(
        <div>
            <RepoInput/>
            <VulnerabilityDisplay/>
        </div>
    )
}